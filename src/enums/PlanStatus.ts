export enum PlanStatus {
  canceled = 'canceled',
  created = 'created',
  errored = 'errored',
  finished = 'finished',
  mfaWaiting = 'mfa_waiting',
  pending = 'pending',
  queued = 'queued',
  running = 'running',
  unreachable = 'unreachable',
}
